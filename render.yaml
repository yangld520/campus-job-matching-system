services:
  - type: web
    name: campus-employment
    runtime: node
    plan: free
    buildCommand: "true"
    startCommand: node server.js
    healthCheckPath: /
    envVars:
      - key: PORT
        value: 3000
